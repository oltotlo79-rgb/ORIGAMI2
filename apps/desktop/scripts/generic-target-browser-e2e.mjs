import { chromium } from 'playwright'
import { spawn } from 'node:child_process'
const origin = 'http://127.0.0.1:4190'
const server = spawn(process.execPath, ['./node_modules/vite/bin/vite.js', '--host', '127.0.0.1', '--port', '4190', '--strictPort'], { cwd: process.cwd(), stdio: 'ignore' })
let browser
try {
  for (let i = 0; i < 150; i += 1) { try { if ((await fetch(origin)).ok) break } catch {}; await new Promise((r) => setTimeout(r, 100)) }
  browser = await chromium.launch({ headless: true }); const page = await browser.newPage()
  await page.goto(`${origin}/scripts/generic-target-browser-harness.html`, { waitUntil: 'networkidle' })
  await page.getByRole('button', { name: 'Recognize image with noise candidate' }).click()
  await page.getByText('Image outline proposal contains 2 parts + 1 possible noise candidate', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Exclude unconfirmed image noise' }).click()
  await page.getByText('Excluded unconfirmed image noise candidate; 2 explicit parts remain', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Correct recognized skeleton endpoint' }).click()
  await page.getByText('Recognized skeleton endpoint corrected to 14 mm before candidate synthesis', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Image meanings unconfirmed: generic topology candidate blocked', { exact: true }).waitFor()
  await page.getByText('Candidate 3 retained unique ID and outline evidence decoded-component-3.', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Restore excluded image candidate' }).click()
  await page.getByText('Restored candidate 3 with original outline evidence; meaning remains unconfirmed', { exact: true }).waitFor()
  await page.getByText('Candidate 3 retained unique ID and outline evidence decoded-component-3.', { exact: true }).waitFor({ state: 'detached' })
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Image meanings unconfirmed: generic topology candidate blocked', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Split image outline component' }).click()
  await page.getByText('Non-authoritative split proposal bound to source image SHA-256', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Outline edit unconfirmed: generic topology candidate blocked', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try tampered outline edit' }).click()
  await page.getByText('Rejected outline edit: source digest or component IDs tampered', { exact: true }).waitFor()
  await page.getByLabel('Vertical split position X (px)').fill('0')
  await page.getByRole('button', { name: 'Confirm outline component edit' }).click()
  await page.getByText('Rejected outline edit: split line misses foreground contour', { exact: true }).waitFor()
  await page.getByLabel('Vertical split position X (px)').fill('50')
  await page.getByRole('button', { name: 'Confirm outline component edit' }).click()
  await page.getByText('Native exact image digest and foreground pixels revalidated; split edit confirmed', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Confirm explicit image part meanings' }).click()
  await page.getByText('Confirmed 3 explicit part meanings for image outlines', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText(/Deterministic candidate synthesis: [3-8] bounded designs from 3 bindings/).waitFor()
  await page.getByRole('button', { name: 'Confirm and apply generic target' }).click()
  const restoredSteps = page.getByRole('list', { name: 'Generated generic feature instruction steps' })
  if (await restoredSteps.getByRole('listitem').count() !== 3) throw new Error('restored candidate instruction count changed')
  for (const [button, status] of [['Undo generic target', 'Generic target undone'], ['Redo generic target', 'Generic target redone'], ['Save and reopen generic target', 'Generic target saved and reopened']]) {
    await page.getByRole('button', { name: button }).click()
    await page.getByText(status, { exact: true }).waitFor()
    if (await restoredSteps.getByRole('listitem').count() !== 3) throw new Error(`${button} lost restored candidate evidence`)
  }
  await page.getByRole('button', { name: 'Reset applied checkpoint' }).click()
  await page.getByRole('button', { name: 'Recognize mixed target GLB' }).click()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('GLB surface meanings unconfirmed: generic topology candidate blocked', { exact: true }).waitFor()
  await page.getByLabel('Assign measured surface range 1').check()
  await page.getByLabel('Assign measured surface range 2').check()
  await page.getByRole('button', { name: 'Try tampered GLB surface range' }).click()
  await page.getByText('Rejected GLB surface assignment: tampered triangle range', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try tampered GLB bulge binding' }).click()
  await page.getByText('Rejected GLB surface assignment: tampered bulge digest or zero direction', { exact: true }).waitFor()
  if (await page.getByLabel('Surface bulge direction Z').inputValue() !== '1') throw new Error('surface bulge direction changed')
  if (await page.getByLabel('Surface bulge amount (mm)').inputValue() !== '5') throw new Error('surface bulge amount changed')
  await page.getByRole('button', { name: 'Confirm GLB surface assignments' }).click()
  await page.getByText('Confirmed two unique GLB surface ranges with digest-bound bulge direction and amount', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByRole('button', { name: 'Confirm and apply generic target' }).click()
  const surfaceSteps = page.getByRole('list', { name: 'Generated generic feature instruction steps' })
  if (await surfaceSteps.getByRole('listitem').count() !== 2) throw new Error('surface range instruction count changed')
  for (const [button, status] of [['Undo generic target', 'Generic target undone'], ['Redo generic target', 'Generic target redone'], ['Save and reopen generic target', 'Generic target saved and reopened']]) {
    await page.getByRole('button', { name: button }).click()
    await page.getByText(status, { exact: true }).waitFor()
    if (await surfaceSteps.getByRole('listitem').count() !== 2) throw new Error(`${button} lost surface range assignments`)
    await page.getByText('Applied GLB surface bulges: ranges 1,2 · direction Z 1 · amount 5 mm · digest and part mapping retained', { exact: true }).waitFor()
  }
  await page.getByRole('button', { name: 'Reset applied checkpoint' }).click()
  await page.getByRole('button', { name: 'Create empty generic target' }).click()
  await page.getByRole('button', { name: 'Assign three candidates the same explicit meaning' }).click()
  await page.getByText('Three exact image candidate IDs assigned explicitly to the same tail meaning', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Confirm explicit image part meanings' }).click()
  await page.getByText('Confirmed 3 explicit part meanings for image outlines', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText(/Deterministic candidate synthesis: [3-8] bounded designs from 3 bindings/).waitFor()
  await page.getByRole('button', { name: 'Confirm and apply generic target' }).click()
  const repeatedSteps = page.getByRole('list', { name: 'Generated generic feature instruction steps' })
  if (await repeatedSteps.getByRole('listitem').count() !== 3) throw new Error('same-meaning instruction count changed')
  for (const [button, status] of [['Undo generic target', 'Generic target undone'], ['Redo generic target', 'Generic target redone'], ['Save and reopen generic target', 'Generic target saved and reopened']]) {
    await page.getByRole('button', { name: button }).click()
    await page.getByText(status, { exact: true }).waitFor()
    if (await repeatedSteps.getByRole('listitem').count() !== 3) throw new Error(`${button} lost same-meaning steps`)
  }
  await page.getByRole('button', { name: 'Reset applied checkpoint' }).click()
  await page.getByRole('button', { name: 'Create empty generic target' }).click()
  await page.getByRole('button', { name: 'Recognize asymmetric bird landmarks' }).click()
  await page.getByText('Asymmetric bird landmarks bound: head · tail · left wing · right wing', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try invalid GLB bounds' }).click()
  await page.getByText('Rejected GLB: non-finite or oversized bounds', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try dense multi-component GLB' }).click()
  await page.getByText('Rejected GLB: dense or multiple components', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try conflicting recognition merge' }).click()
  await page.getByText('Rejected merge: conflicting bounds or part bindings', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try stale recognition merge' }).click()
  await page.getByText('Rejected merge: stale image or GLB asset', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Confirm image and GLB merge' }).click()
  await page.getByRole('button', { name: 'Damage merged authority' }).click()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Merged authority invalid: candidate generation refused', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try oversized decoded image' }).click()
  await page.getByText('Rejected image: decoded pixel resource limit', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try stale decoded image' }).click()
  await page.getByText('Rejected image: stale decoded asset', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try invalid protrusion segmentation' }).click()
  await page.getByText('Rejected segmentation: overlapping or too-thin protrusion', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try noisy silhouette segmentation' }).click()
  await page.getByText('Rejected segmentation: noise exceeds bounded curvature budget', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Recognize low confidence JPEG' }).click()
  await page.getByText('Confidence 42/100 · low_component_ratio, bounded_curvature', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Copy recognized confidence authority' }).click()
  await page.getByText('Low confidence copy blocked without override', { exact: true }).waitFor()
  await page.getByLabel('Override low confidence').check()
  await page.getByRole('button', { name: 'Copy recognized confidence authority' }).click()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Image meanings unconfirmed: generic topology candidate blocked', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Confirm explicit image part meanings' }).click()
  await page.getByText('Confirmed 2 explicit part meanings for image outlines', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Copy recognized confidence authority' }).click()
  await page.getByText('Confidence authority copied: 42/100 · low_component_ratio, bounded_curvature', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Confidence authority witness: 42/100 · low_component_ratio, bounded_curvature', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Cancel generic target grid' }).click()
  await page.getByRole('button', { name: 'Try tampered confidence' }).click()
  await page.getByText('Rejected confidence: tampered score or reason', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Recognize EXIF JPEG silhouette' }).click()
  await page.getByRole('button', { name: 'Confirm explicit image part meanings' }).click()
  await page.getByText('Confidence 88/100 · dominant_component, bounded_simplification_error', { exact: true }).waitFor()
  await page.getByText('Decoded image preview: JPEG RGB · EXIF orientation 6 normalized · body 4 · local 1:3', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try refinement deadline one-short' }).click()
  await page.getByText('Refinement deadline one-short: zero additional seed admitted', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try refinement resource one-short' }).click()
  await page.getByText('Refinement resource one-short: 31/32 proposals accepted safely', { exact: true }).waitFor()
  for (const [button, status] of [
    ['Try unmanufacturable crease spacing', 'Rejected candidate: minimum crease spacing violated'],
    ['Try unmanufacturable face area', 'Rejected candidate: minimum face area violated'],
    ['Try unmanufacturable paper margin', 'Rejected candidate: paper boundary margin violated'],
  ]) {
    await page.getByRole('button', { name: button }).click()
    await page.getByText(status, { exact: true }).waitFor()
  }
  await page.getByRole('button', { name: 'Try uncertified fold path' }).click()
  await page.getByText('Apply blocked: bounded fold path certificate unavailable', { exact: true }).waitFor()
  for (const [button, status] of [
    ['Try colliding certified path', 'Apply blocked: continuous path collision proven'],
    ['Try path resource limit', 'Apply blocked: continuous path resource limit'],
    ['Try stale path certificate', 'Apply blocked: stale continuous path certificate'],
    ['Try tampered path certificate', 'Apply blocked: tampered continuous path certificate'],
    ['Try 10000 path work', 'Apply blocked: continuous path work 10000 exceeds bound'],
    ['Try foreign path issuer', 'Apply blocked: foreign continuous path issuer'],
    ['Try tampered generic feature binding', 'Apply blocked: tampered generic feature binding'],
  ]) {
    await page.getByRole('button', { name: button }).click()
    await page.getByText(status, { exact: true }).waitFor()
  }
  await page.getByText('Deterministic silhouette segmentation: 2 protrusions · binding 1 asymmetric · binding 2 bilateral', { exact: true }).waitFor()
  await page.getByLabel('Accept segmented protrusion 1').uncheck()
  await page.getByLabel('Accept segmented protrusion 2').uncheck()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Rejected segmentation: at least two accepted protrusions required', { exact: true }).waitFor()
  await page.getByLabel('Accept segmented protrusion 1').check()
  await page.getByLabel('Accept segmented protrusion 2').check()
  await page.getByRole('button', { name: 'Reject one side of bilateral binding 2' }).click()
  await page.getByText('Bilateral half rejection canonicalized to asymmetric binding 2', { exact: true }).waitFor()
  if (await page.getByLabel('Symmetry binding 2').inputValue() !== 'none') throw new Error('bilateral half rejection was not canonicalized')
  await page.getByLabel('Accept segmented protrusion 1').check()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Image silhouette grid witness: JPEG RGB · EXIF orientation 6 normalized', { exact: true }).waitFor()
  await page.getByText('Segmented local contour witness: binding 1:3, binding 2:3', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Cancel generic target grid' }).click()
  await page.getByRole('button', { name: 'Confirm image and GLB merge' }).click()
  await page.getByRole('button', { name: 'Try one-short bulge resource' }).click()
  await page.getByRole('button', { name: 'Try 257 GLB landmarks' }).click()
  await page.getByText('Rejected GLB landmarks: 257 exceeds 256 samples', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Try tampered GLB landmark digest' }).click()
  await page.getByText('Rejected GLB landmarks: digest tampered', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Merged authority invalid: candidate generation refused', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Create empty generic target' }).click(); await assertBindings(page)
  const add = page.getByRole('button', { name: 'Add binding' })
  for (let count = 2; count < 8; count += 1) await add.click()
  if (await page.getByRole('list', { name: 'Editable generic target dimensions' }).getByRole('listitem').count() !== 8) throw new Error('eight-binding upper boundary failed')
  if (await add.isEnabled()) throw new Error('ninth binding was not blocked')
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText(/Deterministic candidate synthesis: 8 bounded designs from 8 bindings and \d+ contour points/).waitFor()
  await page.getByRole('button', { name: 'Cancel generic target grid' }).click()
  await page.getByRole('button', { name: 'Try oversized target' }).click()
  if (await page.getByRole('list').count()) throw new Error('oversized target reached UI')
  await page.getByRole('button', { name: 'Try strict dense contour' }).click()
  await page.getByText('Candidate shortage: strict contour placement produced fewer than three safe designs.', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Contour candidate shortage: safe relaxation is required', { exact: true }).waitFor()
  if (await page.getByRole('region', { name: 'Generic target candidate preview' }).count()) throw new Error('shortage reached candidate preview')
  await page.getByRole('button', { name: 'Relax contour to 12 points and regenerate' }).click()
  await page.getByText('Contour relaxed safely to 12 points; alternative grid ready', { exact: true }).waitFor()
  await page.getByText(/Contour placement witness candidate 1: body 12/).waitFor()
  await assertWitnessCanvas(page.getByRole('img', { name: 'Contour placement correspondence candidate 1' }))
  await page.getByRole('button', { name: 'Replace recognized target' }).click()
  page.once('dialog', (dialog) => dialog.accept())
  await page.getByRole('button', { name: 'Review and copy contours to editor' }).click(); await assertBindings(page)
  if ((await page.getByLabel('Local outline points binding 1').inputValue()).split('\n').length !== 5) throw new Error('recognized local contour was not copied')
  await page.getByText('Contour approximation score: 82', { exact: true }).waitFor()
  const editor = page.getByRole('list', { name: 'Editable generic target dimensions' })
  await page.getByLabel('Body outline points').fill('-5,-5\n5,-5\n5,5\n-5,5')
  await page.getByRole('button', { name: 'Apply outline' }).click()
  const symmetricOutline = (await page.getByLabel('Body outline points').inputValue()).replaceAll(' ', '')
  if (symmetricOutline.split('\n').length !== 4 || !symmetricOutline.includes('-5,5')) throw new Error('symmetric body outline was not accepted')
  await page.getByLabel('Body outline points').fill('0,0\n1,0\n1,1')
  await page.getByRole('button', { name: 'Apply outline' }).click()
  await page.getByRole('alert').waitFor()
  await page.getByLabel('Body outline mode').selectOption('general')
  await page.getByLabel('Body outline points').fill('-5,-5\n5,-5\n4,5\n-3,5')
  await page.getByRole('button', { name: 'Apply outline' }).click()
  const generalOutline = (await page.getByLabel('Body outline points').inputValue()).replaceAll(' ', '')
  if (generalOutline.split('\n').length !== 4 || !generalOutline.includes('-3,5')) throw new Error('general asymmetric outline was not accepted')
  const bodyCanvas = page.getByRole('img', { name: 'Body outline preview' })
  await bodyCanvas.focus(); await page.keyboard.press('ArrowRight')
  await page.waitForFunction((before) => document.querySelector('textarea[aria-label="Body outline points"]')?.value.replaceAll(' ', '') !== before, generalOutline)
  await page.getByLabel('Thickness binding 1 (mm)').fill('0')
  if (await page.getByLabel('Thickness binding 1 (mm)').inputValue() !== '5') throw new Error('invalid dimension was accepted')
  await page.getByLabel('Priority binding 1').fill('80')
  await page.getByLabel('Joint binding 1').selectOption('hinge')
  await page.getByLabel('Motion maximum binding 1 (degrees)').fill('45')
  await page.getByLabel('Local outline points binding 1').fill('-2,-1\n2,-1\n0,2')
  await editor.getByRole('button', { name: 'Apply local outline' }).first().click()
  if ((await page.getByLabel('Local outline points binding 1').inputValue()).split('\n').length !== 3) throw new Error('local outline was not accepted')
  await page.getByLabel('Outline to preview').selectOption('1')
  await page.getByRole('img', { name: 'Binding 1 outline preview' }).waitFor()
  const painted = await page.getByRole('img', { name: 'Binding 1 outline preview' }).evaluate((canvas) => {
    const context = canvas.getContext('2d'); return context.getImageData(0, 0, canvas.width, canvas.height).data.some((value) => value !== 0)
  })
  if (!painted) throw new Error('selected local outline was not painted')
  await editor.getByRole('button', { name: 'Move down' }).first().click()
  if (await page.getByLabel('Length binding 1 (mm)').inputValue() !== '27') throw new Error('feature ID changed during reorder')
  if (await page.getByLabel('Part kind binding 1').inputValue() !== 'tail') throw new Error('feature meaning was detached during reorder')
  if (await page.getByLabel('Priority binding 1').inputValue() !== '80') throw new Error('feature priority changed during reorder')
  if (await page.getByLabel('Joint binding 1').inputValue() !== 'hinge') throw new Error('feature joint changed during reorder')
  await page.getByLabel('Mount vertical binding 1 (mm)').fill('12.5')
  await page.getByLabel('Mount fore-aft binding 1 (mm)').fill('-3.5')
  await page.getByLabel('Direction horizontal binding 1').fill('0.75')
  await page.getByLabel('Bilateral spacing binding 2 (mm)').fill('6.5')
  if (await page.getByLabel('Mount vertical binding 1 (mm)').inputValue() !== '12.5'
    || await page.getByLabel('Mount fore-aft binding 1 (mm)').inputValue() !== '-3.5'
    || await page.getByLabel('Direction horizontal binding 1').inputValue() !== '0.75') throw new Error('mount geometry edit was not retained')
  await page.getByLabel('Part kind binding 1').selectOption('wing')
  if (await page.getByLabel('Part kind binding 1').inputValue() !== 'wing') throw new Error('part kind was not editable')
  await page.getByRole('button', { name: 'Add binding' }).click()
  if (await editor.getByRole('listitem').count() !== 3) throw new Error('binding was not added')
  await editor.getByRole('button', { name: 'Remove' }).last().click()
  if (await editor.getByRole('listitem').count() !== 2) throw new Error('binding was not removed')
  if (await editor.getByRole('button', { name: 'Remove' }).first().isEnabled()) throw new Error('two-binding lower bound was not enforced')
  await page.getByLabel('Symmetry binding 1').selectOption('none')
  await page.getByRole('list', { name: 'Bounded generic target binding dimensions' }).getByText(/^Binding 1 · asymmetric single · count 1/).waitFor()
  await page.getByLabel('Length binding 1 (mm)').fill('31.5')
  await page.getByRole('list', { name: 'Bounded generic target binding dimensions' }).getByText(/length 315/).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  const preview = page.getByRole('region', { name: 'Generic target candidate preview' }); await preview.waitFor()
  await preview.getByText('Corrected recognized skeleton endpoint: 14 mm', { exact: true }).waitFor()
  const comparison = page.getByRole('table', { name: 'Strict candidate authority comparison' })
  await comparison.waitFor()
  if (await comparison.getByRole('row').count() !== 4) throw new Error('strict candidate comparison did not retain top 3')
  await comparison.getByText('certified on apply', { exact: true }).first().waitFor()
  await comparison.getByText('80/100', { exact: true }).waitFor()
  await page.getByText('Multi-start refinement: 5 starts · 6/8 iterations · 3 strict improvements · global best score 92', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Use shape-priority metric' }).click()
  await page.getByText('Preset-weighted 2D+3D ranking: shape · winner 1', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Use foldability-priority metric' }).click()
  await page.getByText('Preset-weighted 2D+3D ranking: foldability · winner 2', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Use balanced metric' }).click()
  await page.getByText('Preset-weighted 2D+3D ranking: balanced · winner 3', { exact: true }).waitFor()
  await page.getByText('Deterministic replay digest: seed-v1-5-6-3-92', { exact: true }).waitFor()
  await page.getByText('Manufacturability verified: crease spacing · face area · paper boundary margin', { exact: true }).waitFor()
  await page.getByText('Native foldability admission: global proof + bounded fold path certificate · collision clear', { exact: true }).waitFor()
  await page.getByText('Native cyclic certificate: bounded_certified_pose_graph_path_v1 · SHA-256 58a6d4c1 · thickness 0/0.1/1/3 mm verified', { exact: true }).waitFor()
  await page.getByText('AsymmetricBirdLandmarkBase candidate: four individually bound GLB/image landmarks · native path certified', { exact: true }).waitFor()
  await assertWitnessCanvas(page.getByRole('img', { name: 'Contour placement correspondence candidate 1' }))
  await page.getByRole('button', { name: 'Select contour candidate 2' }).click()
  await page.getByText(/Contour placement witness candidate 2: body 4, local/).waitFor()
  await assertWitnessCanvas(page.getByRole('img', { name: 'Contour placement correspondence candidate 2' }))
  await page.getByRole('button', { name: 'Replace recognized target' }).click(); await preview.waitFor({ state: 'detached' })
  await page.getByRole('button', { name: 'Confirm image and GLB merge' }).click(); await assertBindings(page)
  await page.getByRole('button', { name: 'Recognize asymmetric four-leg landmarks' }).click()
  await page.getByText('Asymmetric four-leg landmarks bound individually', { exact: true }).waitFor()
  const fourLegBindings = page.getByRole('list', { name: 'Asymmetric four-leg landmark bindings' })
  if (await fourLegBindings.getByRole('listitem').count() !== 4) throw new Error('four-leg landmarks were not displayed individually')
  for (const landmark of ['leg-front-left', 'leg-front-right', 'leg-rear-left', 'leg-rear-right']) {
    await fourLegBindings.getByText(new RegExp(`landmark ${landmark}$`)).waitFor()
  }
  await page.getByRole('button', { name: 'Recognize asymmetric insect landmarks' }).click()
  await page.getByText('Asymmetric insect semantic landmarks bound to certified four-ray groups', { exact: true }).waitFor()
  const insectBindings = page.getByRole('list', { name: 'Ordered insect landmark bindings' })
  if (await insectBindings.getByRole('listitem').count() !== 10) throw new Error('ten insect semantic landmarks were not retained')
  await page.getByText('Ray-group digests: ray0 91a70f2c · ray1 a72be019 · ray2 c31488da · ray3 e90f147b', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Recognize asymmetric fish landmarks' }).click()
  await page.getByText('Asymmetric fish semantic landmarks bound to certified four-ray groups', { exact: true }).waitFor()
  const fishBindings = page.getByRole('list', { name: 'Ordered fish landmark bindings' })
  if (await fishBindings.getByRole('listitem').count() !== 4) throw new Error('four fish semantic landmarks were not retained')
  for (const role of ['head', 'tail', 'fin_left', 'fin_right']) await fishBindings.getByText(new RegExp(`: ${role} `)).waitFor()
  await page.getByText('Fish ray-group digests: ray0 63c80a15 · ray1 15ec3972 · ray2 b2481d90 · ray3 e3714a6f', { exact: true }).waitFor()
  await page.getByText('Authority binding: image → body/local contours; GLB → depth/bulge targets.', { exact: true }).waitFor()
  await page.getByRole('region', { name: 'GLB geometry witness' }).getByText(/3D bounds 120×80×65 mm · 2D silhouette difference 7% · bulge targets 2/).waitFor()
  if ((await page.getByLabel('Body outline points').inputValue()).split('\n').length !== 4
    || (await page.getByLabel('Local outline points binding 1').inputValue()).split('\n').length !== 3) throw new Error('GLB contours were not proposed')
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Deterministic replay digest: seed-v1-5-6-3-92', { exact: true }).waitFor()
  await page.getByText('GLB evaluation witness: bounds 120×80×65 mm, silhouette difference 7%, bulges 2', { exact: true }).waitFor()
  await page.getByText('Typed GLB surface landmarks: 4/256 samples · digest 7f3a9c21 · deterministic quantization', { exact: true }).waitFor()
  await page.getByText('Merged authority witness: image contours + GLB depth/bulges', { exact: true }).waitFor()
  await page.getByText('3D candidate score 84/100 · bounded depth error 3 mm', { exact: true }).waitFor()
  await page.getByText('Native folded landmarks: body/local 3D · Hausdorff 4% · depth 3 mm · bulge error 2% · collision clear', { exact: true }).waitFor()
  await page.getByText('AsymmetricFourLegLandmarkBase candidate: four individually bound leg landmarks · native certified mock accepted', { exact: true }).waitFor()
  await page.getByText('AsymmetricInsectLandmarkBase candidate: ten ordered semantic landmarks · four ray-group digests · native path certified', { exact: true }).waitFor()
  await page.getByText('AsymmetricFishLandmarkBase candidate: four ordered semantic landmarks · four ray-group digests · native path certified', { exact: true }).waitFor()
  await page.getByText('Folded face quality: orientation error 6% · area coverage error 9% · manifold faces verified', { exact: true }).waitFor()
  await page.getByText('Landmark error vectors: 4 · maximum error point 3 · combined score 84/100', { exact: true }).waitFor()
  await assertWitnessCanvas(page.getByRole('img', { name: 'Folded target and candidate landmark overlay' }))
  await page.getByRole('button', { name: 'Select contour candidate 2' }).click()
  await page.getByText('Landmark error vectors: 4 · maximum error point 3 · combined score 68/100', { exact: true }).waitFor()
  await assertWitnessCanvas(page.getByRole('img', { name: 'Folded target and candidate landmark overlay' }))
  await page.getByRole('button', { name: 'Cancel generic target grid' }).click(); await preview.waitFor({ state: 'detached' })
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByRole('button', { name: 'Select contour candidate 2' }).click()
  await page.getByRole('button', { name: 'Try tampered skeleton branch graph' }).click()
  await page.getByText('Rejected skeleton graph: cycle, duplicate edge, or branch authority tampered', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Evaluate generic target grid' }).click()
  await page.getByText('Skeleton cycle blocked: simulation proof unavailable', { exact: true }).waitFor()
  await page.getByRole('button', { name: 'Restore confirmed skeleton tree' }).click()
  await page.getByText('Confirmed tree skeleton restored from explicit branch adjacency', { exact: true }).waitFor()
  await page.getByText(/Confirmed tree skeleton: root→1\[feature 1\], 1→2\[feature 2\] · authority [0-9a-f]{8}/).waitFor()
  await page.getByRole('button', { name: 'Confirm and apply generic target' }).click(); await preview.waitFor({ state: 'detached' })
  await page.waitForFunction(() => document.activeElement?.textContent === 'Evaluate generic target grid')
  await page.getByText('Applied contour placement witness candidate 2', { exact: true }).waitFor()
  await page.getByText(/Generic feature topology witness: 1:\d@feature1→skeleton1\.end#crease-[0-9a-f]{8}, 2:\d@feature2→skeleton2\.end#crease-[0-9a-f]{8}/).waitFor()
  await page.getByText(/Persisted tree skeleton mapping: root→1\[feature 1\], 1→2\[feature 2\] · authority [0-9a-f]{8}/).waitFor()
  const genericSteps = page.getByRole('list', { name: 'Generated generic feature instruction steps' })
  if (await genericSteps.getByRole('listitem').count() !== 2) throw new Error('generic feature instruction count changed')
  await genericSteps.getByText(/Shape generated feature 1 · \d certified endpoint creases · skeleton segment 1\.end/).waitFor()
  await genericSteps.getByText(/Shape generated feature 2 · \d certified endpoint creases · skeleton segment 2\.end/).waitFor()
  await page.getByText(/Applied synthesized candidate set: [3-8] bounded designs/).waitFor()
  await page.getByText('Applied image outline evidence + 2 explicitly confirmed part meanings', { exact: true }).waitFor()
  await page.getByText('Applied GLB witness: bounds 120×80×65 mm, bulges 2', { exact: true }).waitFor()
  await page.getByText('Applied merged authority witness: image contours + GLB depth/bulges', { exact: true }).waitFor()
  await page.getByText('Applied 3D candidate score 68/100 · depth error 7 mm', { exact: true }).waitFor()
  await assertWitnessCanvas(page.getByRole('img', { name: 'Applied contour placement correspondence candidate 2' }))
  for (const [button, status] of [['Undo generic target', 'Generic target undone'], ['Redo generic target', 'Generic target redone'], ['Save and reopen generic target', 'Generic target saved and reopened']]) {
    await page.getByRole('button', { name: button }).click(); await page.getByText(status, { exact: true }).waitFor()
    await page.getByText('Applied contour placement witness candidate 2', { exact: true }).waitFor()
    await page.getByText(/Applied synthesized candidate set: [3-8] bounded designs/).waitFor()
    await page.getByText('Applied GLB witness: bounds 120×80×65 mm, bulges 2', { exact: true }).waitFor()
    await page.getByText('Applied typed surface landmarks: 4 samples · digest 7f3a9c21 · archive retained', { exact: true }).waitFor()
    await page.getByText(/Automatic fold instructions: summary \+ \d+ topology-bound generic feature steps/).waitFor()
    await page.getByText('Persisted corrected skeleton endpoint: 14 mm', { exact: true }).waitFor()
    await page.getByText('Applied image outline evidence + 2 explicitly confirmed part meanings', { exact: true }).waitFor()
    if (await genericSteps.getByRole('listitem').count() !== 2) throw new Error('generic feature instructions were not retained')
    await page.getByText('Applied merged authority witness: image contours + GLB depth/bulges', { exact: true }).waitFor()
    await page.getByText('Applied path provenance: bounded_certified_pose_graph_path_v1 · SHA-256 58a6d4c1 · typed archive retained', { exact: true }).waitFor()
    await page.getByText('Applied AsymmetricBirdLandmarkBase: Undo/Redo/reopen retained four landmark bindings and path provenance', { exact: true }).waitFor()
    await page.getByText('Applied AsymmetricFourLegLandmarkBase: Undo/Redo/reopen retained four individual leg landmarks and native path provenance', { exact: true }).waitFor()
    await page.getByText('Applied AsymmetricInsectLandmarkBase: Undo/Redo/reopen retained ten semantic bindings, four group digests, and native path provenance', { exact: true }).waitFor()
    await page.getByText('Applied AsymmetricFishLandmarkBase: Undo/Redo/reopen retained four semantic bindings, four group digests, and native path provenance', { exact: true }).waitFor()
    await page.getByText('Applied 3D candidate score 68/100 · depth error 7 mm', { exact: true }).waitFor()
  }
  for (const format of ['SVG', 'FOLD', 'ORIPA', 'Instruction PDF', 'Instruction SVG ZIP']) {
    await page.getByRole('button', { name: `Export ${format}` }).click()
    await page.getByText(`${format} parsed: topology authority and confidence provenance retained`, { exact: true }).waitFor()
    await page.getByText('AsymmetricFourLegLandmarkBase export retained four individual leg bindings and certified provenance', { exact: true }).waitFor()
    await page.getByText('AsymmetricInsectLandmarkBase export retained ten semantic bindings and four certified ray-group digests', { exact: true }).waitFor()
    await page.getByText('AsymmetricFishLandmarkBase export retained four semantic bindings and four certified ray-group digests', { exact: true }).waitFor()
  }
  await page.getByRole('button', { name: 'Try tampered provenance export' }).click()
  await page.getByText('Rejected export: stale or tampered topology provenance', { exact: true }).waitFor()
  console.log('generic target browser E2E passed: image/GLB, add/remove/reorder bounds, stale/cancel, apply history/save')
} finally { await browser?.close(); server.kill('SIGTERM') }
async function assertBindings(page) {
  const list = page.getByRole('list', { name: 'Bounded generic target binding dimensions' }); await list.waitFor()
  if (await list.getByRole('listitem').count() !== 2) throw new Error('generic binding count changed')
}
async function assertWitnessCanvas(canvas) {
  await canvas.waitFor()
  const painted = await canvas.evaluate((element) => element.getContext('2d').getImageData(0, 0, element.width, element.height).data.some((value) => value !== 0))
  if (!painted) throw new Error('contour placement correspondence was not painted')
}

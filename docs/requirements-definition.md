# ORIGAMI2 要件定義書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | ORIGAMI2 要件定義書 |
| 製品名 | ORIGAMI2（仮称） |
| 文書版 | 0.1.0 |
| 作成日 | 2026-07-16 |
| 対象 | 作家向け初版および初心者向け将来構想 |
| ライセンス候補 | GPL-3.0 |
| ステータス | 初期要件確定、設計前 |

本書は、実装前の対話で決定した要求を、設計・実装・検証に使用できる形へ整理したものである。性能値や厳密判定の実現方式は、技術検証後に設計書で具体化する。

## 2. 目的

### 2.1 初期目的

折り紙作家が、一枚の紙の展開図を精密に作成し、厚さを考慮した3D折りシミュレーションによって完成形と折り過程を検証し、折り手順を制作できるデスクトップアプリケーションを提供する。

### 2.2 長期目的

初心者が参考画像、3Dモデル、部品構成または骨格から作りたい形を指定し、展開図と折り手順を端末内で自動生成できる設計支援アプリケーションへ発展させる。

### 2.3 最優先品質

幾何学的な正確さと折り可能性の判定を最優先とする。操作性、表示品質、処理速度は重要であるが、計算結果の正しさを損なう近似を無断で確定結果として扱わない。

## 3. 製品方針

- Windows 10/11およびmacOSに対応するデスクトップアプリとする。
- 基本機能および将来の自動設計を端末内で実行する。
- 作品データ、利用統計、クラッシュ情報を外部へ自動送信しない。
- サーバーの維持費や有料APIを必要としない。
- ソースコードとリリースをGitHubで公開する。
- GPL-3.0を第一候補とし、商用利用、改造、再配布を許可する一方、配布される派生物には同ライセンスに基づくソース公開を求める。
- macOS版は当初、署名・公証なしで配布し、初回起動手順を案内する。
- 外部試用版は、初版の全必須機能が完成した後に少人数の折り紙作家へ提供する。
- 開発中は機能単位の内部版を作成し、利用者が都度確認する。

## 4. 対象ユーザー

### 4.1 初版

- 主利用者：折り紙作家および高度な展開図を設計する利用者
- 試用者：開発依頼者および少人数の折り紙作家
- UI言語：日本語、英語

### 4.2 将来版

- 折り紙設計の専門知識を持たない初心者
- 画像や3D形状から作品を作りたい利用者

## 5. スコープ

### 5.1 初版の必須範囲

1. 一枚紙の2D展開図編集
2. 精密作図、数式入力、幾何制約
3. 紙厚を考慮した3D表示と折り操作
4. 衝突検出および衝突前停止
5. 局所・全体の折り可能性検証
6. 衝突しない折り経路の厳密探索
7. 折り手順の編集、記録、再生
8. 指先・つまみ位置等の簡略3Dガイド
9. 画像およびPDFの折り図出力
10. 指定された2D・3Dファイル形式の入出力
11. 日本語・英語、テーマ、レイアウト、ショートカット設定
12. 自動保存、クラッシュ復旧、永続Undo/Redo
13. 折り線10,000本以上を想定したデータ構造と編集・表示性能

### 5.2 将来範囲

- 動物・昆虫を対象とした完成形からの自動設計
- 参考画像および3Dモデルからの形状・骨格・部位認識
- 部品構成・骨格編集による目標形状指定
- 展開図と折り手順の完全自動生成
- 膨らみ、突起、関節および可動範囲を含む目標形状制約
- 評価基準と難易度条件を考慮した候補生成
- ペンタブレット、タッチ操作
- GIF、動画、対話型Web形式の手順出力

### 5.3 初版の対象外

- 複数枚の紙
- 接着
- 曲線折りおよび曲線切断
- 折り曲げ半径、紙の弾性、繊維方向、塑性変形の物理計算
- リアルな手・腕の3D表示
- データ収集型テレメトリー
- クラウド計算および運営サーバー

## 6. 機能要件

要件IDの優先度は、`MUST`が初版必須、`FUTURE`が将来要件を表す。

### 6.1 プロジェクトと紙

| ID | 優先度 | 要件 |
|---|---|---|
| PRJ-001 | MUST | 一作品につき、元が一枚である紙を管理できること。 |
| PRJ-002 | MUST | 紙の輪郭を、直線辺からなる任意の単純多角形として定義できること。正方形・長方形に限定しない。 |
| PRJ-003 | MUST | プロジェクトごとに「切断を許可する／許可しない」を設定できること。 |
| PRJ-004 | MUST | 切断許可時は切断線を使用でき、切断禁止時は切断線の作成および切断状態への遷移を禁止すること。 |
| PRJ-005 | MUST | 接着を扱わないこと。 |
| PRJ-006 | MUST | 紙の厚さとして0以上の一定値を設定できること。 |
| PRJ-007 | MUST | 紙の表裏に、個別の単色または画像・模様を設定できること。 |
| PRJ-008 | MUST | 長さの単位をmm、cm、inch、紙の辺を基準とする比率から選択できること。 |
| PRJ-009 | MUST | 頂点、折り線、面に安定した内部IDを付与し、名前、色、メモを設定できること。 |

### 6.2 線種とレイヤー

| ID | 優先度 | 要件 |
|---|---|---|
| LIN-001 | MUST | 山折り、谷折り、補助線、輪郭線、切断線を扱えること。 |
| LIN-002 | MUST | 初版の折り線、切断線および輪郭辺は直線のみとすること。 |
| LIN-003 | MUST | 線種を色と線パターンの両方で識別でき、白黒印刷でも判別可能であること。 |
| LIN-004 | MUST | 折り線、補助線、注釈、下絵等をレイヤーで管理できること。 |
| LIN-005 | MUST | レイヤーごとに表示、非表示、ロック、透明度を設定できること。 |

### 6.3 2D編集

| ID | 優先度 | 要件 |
|---|---|---|
| EDT-001 | MUST | 頂点・線・面を選択、追加、移動、分割、削除できること。 |
| EDT-002 | MUST | マウス操作と数値入力の双方で作図できること。 |
| EDT-003 | MUST | 座標、長さ、角度を指定できること。 |
| EDT-004 | MUST | 小数、分数、平方根、円周率、四則演算、括弧を含む数式を入力できること。 |
| EDT-005 | MUST | 入力した式を保持し、計算時に高精度数値へ変換すること。元の式と評価値を切り替えて表示できること。 |
| EDT-006 | MUST | グリッド、頂点、辺、中点、交点、水平、垂直、平行、角度へスナップできること。 |
| EDT-007 | MUST | 円、角度二等分線、垂線、平行線等、定規とコンパスに相当する補助作図を提供すること。 |
| EDT-008 | MUST | 長さ、角度、水平、垂直、等長、平行、点の線上固定、左右対称、回転対称、角度二等分、長さ比率の幾何制約を設定できること。 |
| EDT-009 | MUST | 制約が矛盾した場合、原因となる制約を特定して表示すること。 |
| EDT-010 | MUST | 対称軸または回転中心を指定し、片側の追加・移動・削除を反対側へリアルタイムに反映すること。 |
| EDT-011 | MUST | 編集中は未完成または幾何学的に不正な展開図の作成・保存を許可すること。 |
| EDT-012 | MUST | 3D折り操作へ移行する前に展開図を検証し、必須条件を満たさない場合は問題位置と理由を表示して移行を禁止すること。 |

### 6.4 検証と折り可能性

| ID | 優先度 | 要件 |
|---|---|---|
| VAL-001 | MUST | 頂点・線の重複、線の交差、未分割交点、閉じていない面、輪郭外要素、接続関係を検証すること。 |
| VAL-002 | MUST | 各頂点について川崎定理、前川定理等の局所的な平坦折り条件を検証すること。適用条件外の場合はその旨を示すこと。 |
| VAL-003 | MUST | 展開図全体が平坦に折り畳めるかを判定すること。 |
| VAL-004 | MUST | 指定状態へ衝突せず到達できる折り経路を厳密に探索できること。 |
| VAL-005 | MUST | 厳密判定中は完了まで処理を継続し、進捗または処理中であることを表示すること。 |
| VAL-006 | MUST | 厳密判定を利用者がいつでも安全に中止できること。中止によって編集データを失わないこと。 |
| VAL-007 | MUST | 判定処理をバックグラウンドで実行し、UIを応答可能に保つこと。 |
| VAL-008 | MUST | 折り不可能と判定された3D操作を実行せず、理由と可能な修正候補を表示すること。 |
| VAL-009 | MUST | 「不可能」「可能」「利用者による中止」「計算エラー」を区別すること。中止を不可能判定として扱わないこと。 |

> 注記：任意の大規模展開図に対する全体平坦折り判定と経路探索は、計算複雑性および実用上の停止性に重大なリスクがある。完全性、対象クラス、証明情報、並列化方式は技術検証で定義し、実現不能な場合は要件変更について合意を得ること。黙って近似判定へ置き換えてはならない。

### 6.5 3D折りシミュレーション

| ID | 優先度 | 要件 |
|---|---|---|
| SIM-001 | MUST | 2D展開図から頂点、辺、面およびヒンジ関係を構築して3D表示できること。 |
| SIM-002 | MUST | 折り線を選択し、角度スライダーおよび数値入力で操作できること。 |
| SIM-003 | MUST | 3D上の紙をマウスでつかんで折る操作ができること。 |
| SIM-004 | MUST | 利用者が固定面を選択し、折り線の反対側につながる面群を連動させられること。 |
| SIM-005 | MUST | 紙の厚さを表示と面同士の衝突・重なり判定に反映すること。 |
| SIM-006 | MUST | 衝突直前で操作を自動停止し、衝突した面、位置、原因を表示すること。 |
| SIM-007 | MUST | 紙の表裏の色・模様を3D表示へ反映すること。 |
| SIM-008 | MUST | 切断後も各部分が元の一枚紙に由来することを保持し、切断された接続関係を正しく反映すること。 |
| SIM-009 | MUST | 折り線10,000本以上のデータを読み込み、表示・選択・基本編集できる設計とすること。具体的な応答時間は性能設計で定義する。 |

### 6.6 折り手順

| ID | 優先度 | 要件 |
|---|---|---|
| INS-001 | MUST | 折り手順をタイムラインとして作成、編集、保存、再生できること。 |
| INS-002 | MUST | 1ステップで複数の折り線を同時に操作できること。 |
| INS-003 | MUST | 各ステップに固定面、持つ位置、押さえる位置、持ち替えを記録できること。 |
| INS-004 | MUST | 折り角度、所要時間、説明文、カメラ位置、矢印、注目箇所、注意事項を保存できること。 |
| INS-005 | MUST | 指先、つまみ位置、押さえ位置、移動方向を簡略的な3Dガイドで表示できること。 |
| INS-006 | MUST | タイムラインへ手動で操作を登録できること。 |
| INS-007 | MUST | 3Dで行った折り操作を自動記録し、後から分割、結合、角度調整できること。 |
| INS-008 | MUST | 中割り折り、かぶせ折り、沈め折り等の複合操作を名前付き折り技法としてまとめられること。 |
| INS-009 | MUST | 利用者が独自の折り技法を作成し、ファイルとして共有できること。 |
| INS-010 | MUST | 折り手順をアプリ内アニメーション、手順ごとの画像、PDF形式で出力できること。 |

### 6.7 ファイル入出力

| ID | 優先度 | 要件 |
|---|---|---|
| IO-001 | MUST | 独自単一ファイル形式`.ori2`でプロジェクトを保存・読込できること。 |
| IO-002 | MUST | `.ori2`に展開図、3D状態、折り手順、紙の見た目、メモ、サムネイル、編集履歴を格納できること。 |
| IO-003 | MUST | 同一内容をJSON、画像等に展開したフォルダー形式でも保存・読込できること。 |
| IO-004 | MUST | SVGおよびFOLD形式を読み込めること。 |
| IO-005 | MUST | SVG読込時、色、線種、属性からアプリ内線種へ割り当てるインポート画面を提供すること。 |
| IO-006 | MUST | SVG、PDF、FOLD、DXFを書き出せること。 |
| IO-007 | MUST | OBJ、STL、glTFで3D完成形を書き出せること。 |
| IO-008 | MUST | Blender等での表示・編集、3Dプリンター利用、Web・他3Dアプリでのアニメーション利用を想定すること。 |
| IO-009 | MUST | 形式が表現できない情報を出力する際、失われる情報を事前に表示すること。 |

### 6.8 編集履歴と復旧

| ID | 優先度 | 要件 |
|---|---|---|
| HIS-001 | MUST | Undo/Redoを提供すること。 |
| HIS-002 | MUST | プロジェクトを閉じて再度開いた後も編集履歴を利用できること。 |
| HIS-003 | MUST | 履歴数または保存容量の上限を利用者が設定できること。 |
| HIS-004 | MUST | 一定間隔で復旧用データを自動保存すること。 |
| HIS-005 | MUST | クラッシュ後に未保存の編集状態を復元できること。 |
| HIS-006 | MUST | 復旧用データによって通常の保存ファイルを直接上書きしないこと。 |

### 6.9 UI・アクセシビリティ

| ID | 優先度 | 要件 |
|---|---|---|
| UI-001 | MUST | 日本語と英語を切り替えられること。 |
| UI-002 | MUST | マウスによる直感操作と精密な数値編集を同一ワークフローで利用できること。 |
| UI-003 | MUST | 2D展開図と3Dプレビューを並列表示またはタブ表示できること。 |
| UI-004 | MUST | パネルの位置と大きさを変更し、作業レイアウトを保存できること。 |
| UI-005 | MUST | ライト、ダーク、OSテーマ連動を提供すること。 |
| UI-006 | MUST | WindowsとmacOS向けの標準ショートカットを用意すること。 |
| UI-007 | MUST | ショートカットを変更でき、重複割り当てを検出できること。 |
| UI-008 | MUST | 初版の入力装置としてマウス、トラックパッド、キーボードをサポートすること。 |
| UI-009 | MUST | 2Dで選択した要素と3Dの対応要素を相互に強調表示すること。 |

### 6.10 更新・診断・公開

| ID | 優先度 | 要件 |
|---|---|---|
| OPS-001 | MUST | GitHub Releasesの更新有無を確認できること。 |
| OPS-002 | MUST | 更新確認を無効化できること。 |
| OPS-003 | MUST | 更新は利用者の明示操作で行い、更新確認時に作品や利用状況を送信しないこと。 |
| OPS-004 | MUST | 診断ログを端末内だけに保存すること。 |
| OPS-005 | MUST | 標準診断ログに作品形状、作品ファイル内容、個人情報を含めないこと。 |
| OPS-006 | MUST | 利用者がログ内容を確認し、GitHub Issuesへ手動添付できること。 |
| OPS-007 | MUST | Windows 10/11用パッケージおよびmacOS用パッケージをGitHub Releasesで配布できること。 |
| OPS-008 | MUST | 署名されていないmacOS版の初回起動方法を文書化すること。 |

## 7. 将来自動設計要件

### 7.1 入力

| ID | 優先度 | 要件 |
|---|---|---|
| AUT-001 | FUTURE | 動物・昆虫を最初の対象カテゴリとすること。 |
| AUT-002 | FUTURE | 頭、胴体、脚、角、耳、翼、尾等の部品を組み合わせて目標形状を指定できること。 |
| AUT-003 | FUTURE | 棒状骨格を描き、長さや太さを設定できること。 |
| AUT-004 | FUTURE | 参考画像または3Dモデルを読み込めること。 |
| AUT-005 | FUTURE | 入力から形状、骨格、部位を自動認識し、利用者が修正できること。 |
| AUT-006 | FUTURE | 突起ごとに本数、長さ、太さ、完成時位置、向き、対称性、曲がり、関節、可動範囲、表裏、優先度を設定できること。 |
| AUT-007 | FUTURE | 3D上で範囲を選択し、膨らむ方向と量を目標形状制約として指定できること。 |

### 7.2 生成と評価

| ID | 優先度 | 要件 |
|---|---|---|
| AUT-101 | FUTURE | 目標形状から一枚紙の展開図と折り手順を自動生成すること。 |
| AUT-102 | FUTURE | 生成処理を外部APIやクラウドを使わず端末内で実行すること。 |
| AUT-103 | FUTURE | 完成形への近さ、折りやすさ、工程数、紙の使用効率等の評価優先度を利用者が設定できること。 |
| AUT-104 | FUTURE | 「完成度優先」「折りやすさ優先」等の評価プリセットを提供すること。 |
| AUT-105 | FUTURE | 工程数、利用可能な折り技法、部位の細かさを個別に指定できること。 |
| AUT-106 | FUTURE | 最初に推奨案を一つ提示し、利用者の要求に応じて追加候補を生成・比較できること。 |
| AUT-107 | FUTURE | 膨らみは目標形状への近似として扱い、初期の自動設計では紙の弾性を計算しないこと。 |

## 8. 非機能要件

### 8.1 正確性

- 幾何計算には用途に応じた頑健な数値処理を用いる。
- 数式入力の原文を失わず、高精度評価値と関連付ける。
- 許容誤差を一箇所で管理せず、作図、トポロジー判定、衝突判定、表示に適した値を明示する。
- 判定結果には、可能な限り根拠、対象要素、誤差条件を付ける。

### 8.2 性能

- 一般的な家庭用PCを基準とする。
- 基本的な2D編集は専用GPUを必須としない。
- 3D表示および適用可能な計算にはGPUを利用し、利用できない環境では代替処理を提供する。
- 折り線10,000本以上のプロジェクトで、読み込み、表示、選択、基本編集が可能であることを目標とする。
- 厳密な全体判定は長時間化を許容するが、UIをブロックせず中止可能にする。
- 具体的な基準PC、データセット、応答時間、メモリ上限は性能設計書で確定する。

### 8.3 可搬性

- WindowsとmacOSで共通のプロジェクトファイルを使用できること。
- OS固有処理を計算コアから分離すること。
- 将来のLinux、Web、タブレット対応を妨げないデータモデルとすること。ただし初版では保証しない。

### 8.4 信頼性

- 長時間計算の中止、アプリ異常終了、保存失敗で既存の正常なプロジェクトを破損しないこと。
- 保存時は一時ファイルと置換等を用い、途中書き込みによる破損を防ぐこと。
- 未知フィールドを含むプロジェクトの扱いとバージョン移行方針を定義すること。

### 8.5 プライバシーとセキュリティ

- 作品データを端末外へ自動送信しない。
- 利用統計およびクラッシュレポートを自動送信しない。
- 読み込むSVG、画像、3Dモデル、プロジェクトファイルを信頼できない入力として検証する。
- 更新確認以外のネットワーク通信を初版の通常機能に必要としない。

### 8.6 保守性

- 幾何・トポロジー・制約・シミュレーション・ファイル変換・UIを分離する。
- 計算コアはUIから独立して自動テスト可能にする。
- 日本語・英語の文言をソースコードへ直接埋め込まず、翻訳資源として管理する。
- ファイル形式にはバージョン番号を持たせ、移行処理を提供する。

## 9. データ概念モデル

初版の内部モデルは、少なくとも以下を表現する。

```text
Project
├─ metadata / formatVersion / settings
├─ Paper
│  ├─ boundary
│  ├─ thickness
│  └─ front/back appearance
├─ CreasePattern
│  ├─ vertices
│  ├─ edges (mountain/valley/auxiliary/boundary/cut)
│  ├─ faces
│  ├─ constraints
│  └─ layers
├─ FoldState
│  ├─ fixed faces
│  ├─ fold angles
│  ├─ transforms
│  └─ overlap/collision state
├─ InstructionTimeline
│  ├─ steps
│  ├─ techniques
│  ├─ camera/annotations
│  └─ grip guides
├─ Assets
└─ History / Recovery metadata
```

将来自動設計では、同じモデルの上流へ`TargetShape`、`Skeleton`、`Part`、`DesignConstraint`、`Candidate`、`EvaluationProfile`を追加する。作家モードと初心者モードで別形式を作らない。

## 10. 画面要件

初期レイアウトは次を基本とし、利用者が変更可能とする。

```text
┌──────────┬─────────────────┬─────────────────┐
│ 作図ツール │ 2D展開図        │ 3Dプレビュー     │
│            │                 │                  │
├──────────┴─────────────────┴─────────────────┤
│ 折り手順タイムライン                              │
├───────────────────────────┬──────────────────┤
│ 状態・検証・警告                 │ 選択要素・数値・制約 │
└───────────────────────────┴──────────────────┘
```

主要画面は以下とする。

1. プロジェクト作成・紙設定
2. 2D展開図編集
3. 3D折りシミュレーション
4. 検証結果・長時間計算
5. 折り手順タイムライン
6. 画像・PDF出力
7. ファイルインポート・エクスポート
8. 言語、テーマ、ショートカット、更新等の設定

## 11. 初版の受け入れ条件

初版は、次の条件をすべて満たした時点で外部試用可能とする。

1. 本書の`MUST`要件が実装済みである。
2. Windows 10/11とmacOSで、同一`.ori2`ファイルを作成・保存・再読込できる。
3. 任意の直線多角形の紙に展開図を作成し、切断可否の設定が機能する。
4. 数式入力、スナップ、指定された幾何制約、リアルタイム対称編集が機能する。
5. 3D移行前検証が不正データを検出し、問題要素と理由を示す。
6. 折り線の数値操作と3Dドラッグ操作が機能し、固定面を選択できる。
7. 設定した紙厚が表示および衝突判定へ反映され、衝突前に停止する。
8. 全体平坦折り判定と折り経路探索が、設計時に定義した対応クラスについて正しい結果と根拠を返す。
9. 長時間計算中もUIが応答し、安全に中止できる。
10. 複数折り線、固定面、持ち位置を含む手順を作成・記録・編集・再生できる。
11. 手順画像とPDFを出力できる。
12. 指定された2Dおよび3D形式の入出力試験を通過する。
13. 自動保存後のクラッシュを模擬し、未保存状態を復元できる。
14. 既知の基本展開図による自動テストと、実物の紙による比較検証を通過する。
15. 標準性能試験用の10,000本以上のデータで、性能設計書の基準を満たす。
16. アプリが作品データ、利用統計、クラッシュ情報を外部送信しないことを確認する。
17. 日本語・英語、ライト・ダーク、主要ショートカットが両OSで機能する。

## 12. 検証方針

### 12.1 自動検証

- 幾何プリミティブの単体テスト
- 頑健な交差・包含・向き判定テスト
- 制約充足・矛盾検出テスト
- 川崎・前川条件の既知例と反例
- 面抽出、切断、トポロジー更新の性質テスト
- 順運動学・逆操作の再現性テスト
- 厚さを考慮した衝突判定テスト
- 既知の平坦折り可能・不可能パターン
- ファイル形式の往復変換テスト
- 保存中断・クラッシュ復旧テスト
- 10,000本以上の性能回帰テスト

### 12.2 実物検証

- 基本折り、伝承作品、既知の複雑作品を実際の紙で折る。
- アプリの面位置、重なり順、停止位置、完成形と実物を比較する。
- 差異を計算不具合、紙厚モデル、弾性・塑性等の対象外物理要因に分類する。

### 12.3 試用

- 全必須機能完成後、GitHub Releasesで限定的なテスト版を配布する。
- 少人数の折り紙作家がGitHub Issuesへ問題・要望を登録する。
- 作品ファイルやログは試用者が内容を確認し、明示的に添付した場合だけ共有する。

## 13. 開発上の主要リスク

| リスク | 影響 | 対応方針 |
|---|---|---|
| 任意展開図の全体平坦折り判定 | 完全判定が現実的時間で終了しない可能性 | 対応クラスと証明可能範囲を技術検証し、結果種別を厳密に区別する |
| 衝突のない折り経路探索 | 高次元かつ組合せ的で長時間化 | 中止可能なバックグラウンド探索、状態保存、探索アルゴリズムの交換可能化 |
| 紙厚とヒンジの矛盾 | 厚さゼロの折り軸では物理的に重なる | 初版モデルの定義を設計書で明示し、弾性・曲げ半径とは区別する |
| 10,000本以上の編集と3D | メモリ・描画・判定性能低下 | 空間索引、差分更新、GPU描画、段階的詳細度、性能回帰試験 |
| 多形式の互換性 | 情報欠落や意味の差異 | 変換能力表、警告、往復試験、独自形式を正本とする |
| 全機能完成後まで外部試用しない | 作家の操作要求を発見する時期が遅れる | 依頼者による機能単位の内部確認を必須とする |
| 未署名macOSアプリ | Gatekeeperによる起動障壁 | 明確な初回起動手順とハッシュを公開する |
| GPL依存関係 | 非互換ライセンス混入 | 依存追加時にライセンス検査を行う |

## 14. 未確定事項と設計時の決定事項

以下は要求ではなく、設計または技術検証で決定する。

1. UIフレームワーク、実装言語、幾何計算ライブラリ
2. `.ori2`の内部圧縮・スキーマ・バージョン移行方式
3. 厚さを持つ紙のヒンジおよび重なりの数学モデル
4. 全体平坦折り判定が保証する対象クラス
5. 経路探索の完全性、証明情報、並列化・GPU利用範囲
6. 10,000本性能試験の基準PCと具体的な応答時間
7. DXF、OBJ、STL、glTF、FOLD各形式の対応バージョンと変換能力
8. PDF折り図のページ構成と図記号規約
9. 正式製品名および商標確認
10. GPL-3.0-onlyまたはGPL-3.0-or-laterの最終選択

## 15. 変更管理

- 本書の`MUST`要件を変更する場合は、変更理由、影響範囲、受け入れ条件の変更を記録する。
- 技術的に実現困難な要件は、実装者の判断だけで削除または近似へ変更しない。
- 内部確認で合意した変更は、設計・実装より先に本書へ反映する。
- 将来要件を初版へ移す場合は、性能・配布・テストへの影響を再評価する。

